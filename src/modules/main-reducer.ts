import {
  Game,
  ScoreBoard,
  GameAction,
} from "../types";
function reducer(state: ScoreBoard, action: GameAction): ScoreBoard {
  switch (action.type) {
    default:
      return state;
  }
}

export default reducer;
